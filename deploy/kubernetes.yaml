apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker-test-service123
  namespace: default
  labels:
    app: docker-test-service123
spec:
  replicas: 2
  selector:
    matchLabels:
      app: docker-test-service123
  template:
    metadata:
      labels:
        app: docker-test-service123
    spec:
      containers:
      - name: docker-test-service123
        image: asia-south1-docker.pkg.dev/dev-zephyr-352206/docker-test-service123/docker-test-service123:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: OPENWEATHER_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: openweather-api-key
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: docker-test-service123-service
  namespace: default
spec:
  selector:
    app: docker-test-service123
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer
